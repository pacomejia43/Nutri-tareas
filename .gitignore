*.iml
.gradle/
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build
/app/release
*.hprof
.kotlin/

# Not committed as a binary - see README.md "Compilar en Android Studio" for how it's
# bootstrapped (Android Studio does this on its own; command-line users run `gradle wrapper`).
/gradle/wrapper/gradle-wrapper.jar

# The real release key lives only as GitHub Actions secrets now (see release-signing/README.md).
# This holds a throwaway keystore build.gradle.kts generates once per checkout for local builds.
/release-signing/local/
